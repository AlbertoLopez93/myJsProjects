/****************************************************************************

timestamp: 1472842800000
ArraysEx1.js

Create a function called createCoords that receives an integer N >= 0 and returns an array
with N elements where each element represents a point in a cartesian plane
The points must be filled with a random number with the following conditions:
   x axis = random number between prevX + 1 and prevX+100; taking 0 as base where no prevX is present
   y axis = random number betwen 0 and 100

****************************************************************************/


"use strict";

function createCoords(n) {

}