/****************************************************************************

timestamp: 1472842800000
Name: ArraysEx2.js

Create a function called isInLine that receives an array of points
wich are ordered over a cartesian plane.
Each point is an array of 2 elements [x, y], where both are a numeric value

The function must return a boolean value indicating if all of the points are
in the same line or not

****************************************************************************/


"use strict";


function isInLine(points) {
  
}